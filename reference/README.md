# Local Aurora definitions

Place `AuroraElements.xml` here after obtaining the file that matches the
Aurora installation used for testing. It is ignored by Git because it is a
tooling dependency rather than project source.

